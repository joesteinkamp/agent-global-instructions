# GENERATED from commands/flow.md by render-commands.sh — do not edit.
description = "Generate a user-flow / sitemap / journey-map artifact from a task or spec — served HTML"

prompt = '''
Design refs: !{ls PRODUCT.md DESIGN.md 2>/dev/null || true}

Produce a **flow artifact** for the feature/task in {{args}} — a user flow (default),
a sitemap, or a journey map if named. Design deliverables, not chat.

1. **Resolve the subject & type** — a described feature/task or a spec path, and which
   artifact (default: user flow). Read `PRODUCT.md`/`DESIGN.md` for the target user,
   entry points, and success metrics if present.
2. **Model the flow** — cover the **happy path plus the key branches**: entry points,
   decision points, error/edge states, and exits. Don't flatten it to the happy path.
3. **Build the artifact:**
   - **Diagram** — a flow/sitemap rendered inline (Mermaid or hand-rolled SVG). Keep the
     diagram data self-contained — no network dependency for the content itself.
   - **Journey table** (for a journey map, or alongside a flow) — one row per stage:
     *stage · user action · system response · user thoughts/emotion · pain points ·
     opportunities.*
4. Ground it in the briefs where they exist; where they don't, state the assumptions you
   made so I can correct them.

**Output is an artifact.** Write `flows/<slug>-YYYY-MM-DD/report.html` (self-contained:
the diagram + any journey table). Serve it per my preview method (headless → static
server on `0.0.0.0`, verify 200, hand me the URL; keep it running). Inline, give me a
one-paragraph summary + the link — don't paste the flow into chat.
'''
