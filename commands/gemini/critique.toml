# GENERATED from commands/critique.md by render-commands.sh — do not edit.
description = "Pre-pixel heuristic critique of a flow, spec, or idea — severity-ranked findings with citations"

prompt = '''
Design refs: !{ls PRODUCT.md DESIGN.md 2>/dev/null || true}

**Critique** the flow, spec, or idea in {{args}} *before there are pixels*. This is
the pre-pixel complement to `/audit` (which needs a screenshot) and `/verify` (which
needs a running app): reason about the concept, the flow logic, and the copy — not the
rendering. (To *generate* a flow rather than critique one, use `/flow`.)

1. **Resolve the subject** — a path to a spec/flow doc, or the flow described in the
   arguments. Read `PRODUCT.md`/`DESIGN.md` for intent, target user, and success metrics
   if present, and judge against *that* intent, not generic taste.
2. **Evaluate against named heuristics** — cite the principle for every finding:
   - **Nielsen's 10** — status visibility, match to the real world, user control/undo,
     consistency, error prevention, recognition over recall, flexibility, minimalism,
     error recovery, help.
   - **Gestalt & IA** — grouping, hierarchy, and whether the information architecture and
     step order match the user's mental model.
   - **Cognitive load** — Fitts's/Hick's law, step count, and decisions asked of the user.
   - **Error prevention & recovery** — failure states, dead ends, destructive actions,
     and the paths back.
   - **Concept-level accessibility** — reliance on color/motion alone, reading level, and
     whether the flow works without a mouse (judged at the concept level, not pixels).
   - **Dark patterns** — Cialdini-style pressure, forced continuity, confirmshaming,
     misdirection — flag any.
3. For each finding: **severity** (blocker/major/minor), the **principle** cited, **where**
   in the flow, and a **concrete fix**.

**Output is an artifact.** Write `critiques/<slug>-YYYY-MM-DD/report.html` (self-contained,
severity-ranked table with citations). Serve it per my preview method (headless → static
server on `0.0.0.0`, verify 200, hand me the URL; keep it running). Inline, give me the
top findings + the link — don't dump the findings in chat. This is a review pass —
**don't change anything unless I ask.**
'''
