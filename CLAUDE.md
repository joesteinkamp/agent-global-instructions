<!-- Pointer file: the project instructions live in AGENTS.md, so every tool reads
     one copy. Do NOT paste the global instruction render in here — `customize.sh
     --project` refuses to write into this checkout for the same reason. -->

@AGENTS.md
